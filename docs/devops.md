# 🖥️ 部署运维 Skills

> 来源：🟢 Hermes | 共 5 个 skill

---

## 1. `deploy-java-webapp-macos`

**功能**：Deploy Java web applications (Spring Boot, JSP, etc.) on macOS Apple Silicon — JDK, MySQL, Redis, frontend build, and common pitfalls.

**状态**：🔄 待测试

---

## 2. `kanban-orchestrator`

**功能**：Decomposition playbook + anti-temptation rules for an orchestrator profile routing work through Kanban. The "don't do the work yourself" rule and the basic lifecycle are auto-injected into every kanban worker's system prompt; this skill is the deeper playbook when you're specifically playing the orchestrator role.

**状态**：🔄 待测试

---

## 3. `kanban-worker`

**功能**：Pitfalls, examples, and edge cases for Hermes Kanban workers. The lifecycle itself is auto-injected into every worker's system prompt as KANBAN_GUIDANCE (from agent/prompt_builder.py); this skill is what you load when you want deeper detail on specific scenarios.

**状态**：🔄 待测试

---

## 4. `vite-frontend-deployment`

**功能**：Deploy/restart Vite-based frontend projects (React, Vue, etc.) for preview/sharing. Production build + preview, not dev mode.

**状态**：🔄 待测试

---

## 5. `webhook-subscriptions`

**功能**：Webhook subscriptions: event-driven agent runs.

**状态**：🔄 待测试

---
